import {NgDocPage} from '@ng-doc/core';
import { AsyncPipeComponent } from '../../components/async-pipe/async-pipe.component';

const AsyncPipePage: NgDocPage = {
	title: `5 - AsyncPipe`,
	mdFile: './index.md',
	demos: {AsyncPipeComponent}
};

export default AsyncPipePage;
